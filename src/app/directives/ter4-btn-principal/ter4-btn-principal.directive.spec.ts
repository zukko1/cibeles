import { Ter4BtnPrincipalDirective } from './ter4-btn-principal.directive';

describe('Ter4BtnPrincipalDirective', () => {
  it('should create an instance', () => {
    const directive = new Ter4BtnPrincipalDirective();
    expect(directive).toBeTruthy();
  });
});
